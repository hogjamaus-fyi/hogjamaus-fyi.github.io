name: Mirror to Secondary Repo

on:
  push:
    branches-ignore: []
    tags-ignore: []
  delete:

jobs:
  mirror-and-update-cname:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Source Repository
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
          # Use secrets.MIRROR_PAT if across different accounts/orgs,
          # or secrets.GIT_HUB_TOKEN if inside the same org/user scope.
          token: ${{ secrets.MIRROR_PAT || secrets.GIT_HUB_TOKEN }}

      - name: Configure Git Credentials
        run: |
          git config --global user.name "github-actions [bot]"
          git config --global user.email "268903590+hogjamaus-fyi@users.noreply.github.com"

      - name: Set Remote HTTPS URL
        run: |
          # Uses internal HTTPS auth using the PAT or GITHUB_TOKEN configured above
          git remote add mirror https://x-access-token:${{ secrets.MIRROR_PAT || secrets.GIT_HUB_TOKEN }}@github.com/hogjamaus-fyi/hogjamaus-fyi.github.io.mirror.git

      - name: Process Deletion Event
        if: github.event_name == 'delete'
        run: |
          REF_NAME="${{ github.event.ref }}"
          REF_TYPE="${{ github.event.ref_type }}"

          if [ "$REF_TYPE" = "branch" ]; then
            git push mirror --delete "$REF_NAME" || true
          elif [ "$REF_TYPE" = "tag" ]; then
            git push mirror --delete refs/tags/"$REF_NAME" || true
          fi

      - name: Process CNAME Files and Push (On Push Event)
        if: github.event_name == 'push'
        run: |
          if [ -f "CNAME.mirror.one" ]; then
            mv CNAME.mirror.one CNAME
          else
            echo "hogjamaus.fyi" > CNAME
            echo "www.hogjamaus.fyi" >> CNAME
          fi

          git add CNAME
          git commit -m "Update CNAME for mirror" || echo "No CNAME changes to commit"
          git push --force mirror HEAD:${{ github.ref }}
